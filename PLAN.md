# 한동대학교 온라인 도너월

기술 이름(slug): `handong-donor-wall`

## 한 줄 소개
한동대 후원자가 이름을 검색하면 갈대상자 컨셉의 감사 메시지를 받는 기부자 감사 웹앱

## 핵심 흐름
1. 후원자가 사이트에 접속해 자신의 이름을 입력한다
2. 기부 금액 기준으로 맞춤 감사 화면이 표시된다 (소형: 갈대상자 이미지, 대형: 사진+스토리)
3. "내 후원이 한동의 인재를 키웠다"는 것을 시각적으로 체감하고, 이미지를 저장한다

## 참고 앱/사이트
없음 (갈대상자 컨셉 — 출애굽기 2:3 기반. 이름이 갈대상자 위에 새겨지는 시각 경험)

## 설정
- 로그인: 관리자만 (후원팀 직원)
- LLM API: 불필요 (갈대상자 이미지는 템플릿 위에 이름 텍스트 합성으로 구현)
- 외부 연동: Supabase Storage (대형 후원자 사진 업로드)
- 민감정보: 없음

## 디자인
- 강조색: `#004374` (딥 네이비) — 버튼·링크·강조 / 골드 포인트: `#C6A96F` — 장식·보조 강조
- 배경: `#fbf9f8` (크림) · 글자: `#1b1c1c` · 카드: `#ffffff` · 테두리: `#E2E2E2`
- 상태색: 성공 `#16A34A` · 오류 `#ba1a1a`
- 폰트: Noto Serif (헤딩) + Noto Sans KR (본문·UI)
- 모서리(radius): 8px · 그림자: 옅게 카드에만 (`rgba(0,91,154,0.08)`) · 간격: 넉넉하게
- 레이아웃 원칙: 조회 화면 — 중앙 1단 히어로, 감사 화면 — 중앙 카드, 관리 화면 — 상단 액션바 + 테이블
- 출처: design/covenant_heritage/DESIGN.md (Stitch "Covenant Heritage" 테마)

## 화면
1. **기부자 조회** — `/`
   - 보임: "한동대학교 온라인 도너월" 타이틀 + 갈대상자 소개 문구(1순위) → 이름 검색창 → '감사 메시지 보기' 버튼
   - 동작: 이름 입력 → 검색 버튼 클릭
   - 데이터: 읽음: donors
   - 상태: 로딩(찾는 중) / 이름 없음("해당 이름의 후원자를 찾지 못했습니다") / 정상 이동
   - 디자인: design/_1/code.html

2. **감사 화면 — 소형 후원자** — `/donor/[name]` (기부 금액 < 1천만 원)
   - 보임: 갈대상자 이미지에 후원자 이름 각인(1순위) → 감사 메시지 → 출애굽기 2:3 말씀 → '이미지 저장하기' 버튼
   - 동작: 갈대상자 이미지 PNG 다운로드
   - 데이터: 읽음: donors
   - 상태: 로딩 / 표시(이름 합성 완료)
   - 디자인: design/_2/code.html

3. **감사 화면 — 대형 후원자** — `/donor/[name]` (기부 금액 ≥ 1천만 원)
   - 보임: 후원자 사진(1순위, 크고 둥글게) → 이름 + 기부 스토리 → 한동대 감사 메시지
   - 동작: 없음 (읽기 전용)
   - 데이터: 읽음: donors
   - 상태: 로딩 / 표시
   - 디자인: design/_3/code.html

4. **관리자 로그인** — `/admin/login`
   - 보임: 이메일·비밀번호 입력 폼(1순위) → '로그인' 버튼
   - 동작: Supabase Auth 로그인
   - 데이터: Supabase Auth
   - 상태: 제출 중 / 오류(잘못된 비밀번호 메시지) / 성공(관리 목록으로 이동)
   - 디자인: design/_4/code.html

5. **기부자 관리 목록** — `/admin`
   - 보임: 기부자 테이블(이름·금액·구분·등록일, 1순위) → '기부자 추가' 버튼 → 검색창
   - 동작: 기부자 추가·수정·삭제
   - 데이터: 읽음: donors
   - 상태: 로딩 / 0건("등록된 후원자가 없습니다. 기부자 추가 버튼을 눌러주세요.") / 목록 표시
   - 디자인: design/_5/code.html

6. **기부자 등록/수정 폼** — `/admin/donors/new` & `/admin/donors/[id]/edit`
   - 보임: 이름·기부금액 입력(1순위) → 사진 업로드(대형만) → 스토리 입력(대형만) → '저장하기' 버튼
   - 동작: 신규 등록 또는 수정 저장
   - 데이터: 씀: donors + Storage(사진)
   - 상태: 제출 중 / 성공(목록으로 이동) / 오류(저장 실패 메시지)
   - 디자인: design/_6/code.html

## 데이터 (Supabase 테이블)
- **donors**
  - `id` uuid — 기본 키
  - `name` text — 후원자 이름
  - `donation_amount` int — 기부 금액 (원 단위)
  - `photo_url` text — 대형 후원자 사진 URL (nullable, Supabase Storage)
  - `story` text — 기부 스토리 (nullable)
  - `is_active` boolean — 표시 여부 (기본 true)
  - `created_at` timestamptz
  - 관계: 없음

## 기술 스택
Next.js (App Router, TypeScript) · Tailwind CSS · shadcn/ui · Supabase · Vercel

## MVP 범위
- 포함: 이름 검색, 소형 감사 화면(갈대상자 이미지+이름 합성), 대형 감사 화면(사진+스토리), 관리자 로그인, 기부자 CRUD
- 제외 — 다음에: 기부 금액 티어 추가(예: 5천만 원 이상 별도 디자인), SNS 공유 버튼, 이메일 자동 발송, 다국어(영문) 지원, 기부자 랭킹/통계 페이지

## 진행 상황
- [x] 기획 완료
- [ ] Stitch 프로토타입
- [ ] 연결 (GitHub · Vercel · Supabase)
- [ ] 구현: 기부자 조회 화면
- [ ] 구현: 감사 화면 (소형 후원자 — 갈대상자 이미지)
- [ ] 구현: 감사 화면 (대형 후원자 — 사진+스토리)
- [ ] 구현: 관리자 로그인
- [ ] 구현: 기부자 관리 목록
- [ ] 구현: 기부자 등록/수정 폼
- [ ] 배포 확인
